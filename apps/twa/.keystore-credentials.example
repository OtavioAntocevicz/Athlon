# Copie para .keystore-credentials (gitignored) e ajuste as senhas.
# Usado pelo script de build local — NÃO commitar.

BUBBLEWRAP_KEYSTORE_PASSWORD=troque-esta-senha
BUBBLEWRAP_KEY_PASSWORD=troque-esta-senha
